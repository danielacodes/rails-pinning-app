<% if !@errors.nil? %>
  <div class="error">
    <label class="error-label"><%= pluralize(@errors.count, 'error') %> prohibited this pin from being saved:</label>
    <ul>
      <% @errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>